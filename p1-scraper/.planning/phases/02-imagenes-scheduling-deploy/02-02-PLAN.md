---
phase: 02-imagenes-scheduling-deploy
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - p1-scraper/src/config.ts
  - p1-scraper/src/cron.ts
  - p1-scraper/ecosystem.config.cjs
  - p1-scraper/deploy.sh
  - p1-scraper/package.json
  - p1-scraper/.env.example
autonomous: false
requirements: [OPS-01, OPS-02]
user_setup:
  - service: vps
    why: "Deploy runs against the live VPS and PM2 — requires SSH access the executor cannot self-authenticate"
    env_vars:
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Inherited on VPS from /opt/hub/.env.local (copied by deploy.sh into /opt/p1-scraper/.env.local if missing)"
    dashboard_config:
      - task: "Create public Storage bucket 'p1-images' if the createBucket API call did not (fallback)"
        location: "Supabase dashboard → Storage → New bucket → name p1-images, public"
must_haves:
  truths:
    - "config.ts loads creds from the FIRST existing of [<projectRoot>/.env.local, <projectRoot>/../.env.local], so it works both standalone on the VPS (/opt/p1-scraper/.env.local) and nested in the hub locally (hub/.env.local)"
    - "A long-running cron process (src/cron.ts) schedules the scrape pipeline once per day at 06:00 and stays alive under PM2"
    - "ecosystem.config.cjs defines a PM2 app named p1-scraper running the cron process via tsx with autorestart, max_memory_restart, and log files under /var/log"
    - "deploy.sh rsyncs p1-scraper to root@148.230.72.17:/opt/p1-scraper (excluding node_modules and .env.local), runs npm ci, copies Supabase creds from /opt/hub/.env.local into /opt/p1-scraper/.env.local if missing, and pm2 start/restart + pm2 save"
    - "The deploy 'health' check retries (up to ~20s) until pm2 shows p1-scraper online AND its restart count is stable (not crash-looping) — it is a cron worker, not an HTTP server, so no curl /health"
    - "After ./deploy.sh, pm2 list shows p1-scraper online and pm2 logs show the cron scheduler started"
  artifacts:
    - path: "p1-scraper/src/config.ts"
      provides: "Portable env loading: array of candidate .env.local paths + existsSync, fail-fast validation"
      contains: "existsSync"
    - path: "p1-scraper/src/cron.ts"
      provides: "node-cron daily schedule invoking the scrape pipeline; long-lived PM2 process"
      contains: "cron.schedule"
    - path: "p1-scraper/ecosystem.config.cjs"
      provides: "PM2 app definition for p1-scraper running the cron process"
      contains: "p1-scraper"
    - path: "p1-scraper/deploy.sh"
      provides: "Reproducible rsync + npm ci + PM2 deploy to the VPS"
      contains: "rsync"
  key_links:
    - from: "p1-scraper/ecosystem.config.cjs"
      to: "p1-scraper/src/cron.ts"
      via: "PM2 script/args run tsx src/cron.ts"
      pattern: "cron"
    - from: "p1-scraper/src/cron.ts"
      to: "the scrape pipeline"
      via: "imports and calls the run-scrape main (exported) or spawns tsx src/run-scrape.ts"
      pattern: "run-scrape|runScrape|spawn"
    - from: "p1-scraper/deploy.sh"
      to: "VPS /opt/p1-scraper"
      via: "rsync over ssh + remote npm ci + pm2 start ecosystem.config.cjs"
      pattern: "/opt/p1-scraper"
    - from: "p1-scraper/deploy.sh"
      to: "p1-scraper/src/config.ts env loading"
      via: "writes creds to /opt/p1-scraper/.env.local (config.ts candidate #1 on VPS)"
      pattern: "/opt/p1-scraper/.env.local"
---

<objective>
Make the scraper run autonomously once per day on the VPS under PM2. First make
env loading portable (config.ts), then build the node-cron worker (`src/cron.ts`),
the PM2 app definition (`ecosystem.config.cjs`), and a reproducible `deploy.sh`
adapted from cotizador-bot — then deploy to the VPS and verify the process is
online (human-action checkpoint, since it touches the live VPS over SSH).

Purpose: the scraping + image-mirroring pipeline from Plan 01 must run unattended
daily because p1travel prices are dynamic.
Output: src/config.ts (portable env), src/cron.ts, ecosystem.config.cjs, deploy.sh,
package.json (cron script), .env.example, and a live deployment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

IMPORTANT: File paths are relative to the HUB ROOT (/Users/ezequielmayoni/hub);
the executor runs from there. So `p1-scraper/src/cron.ts` is the real path. On the
VPS the app lives STANDALONE at `/opt/p1-scraper` (NOT nested in /opt/hub), and
creds are sourced from `/opt/hub/.env.local` but WRITTEN to `/opt/p1-scraper/.env.local`.
This is a NODE runtime deploy (tsx), NOT Python. The p1-scraper folder is untracked
inside the hub repo — deploy.sh rsyncs the folder contents directly.
</execution_context>

<context>
@p1-scraper/.planning/PROJECT.md
@p1-scraper/.planning/ROADMAP.md

<reference_patterns>
<!-- Adapt these; do NOT copy the Python/uvicorn/Playwright specifics. -->

cotizador-bot/ecosystem.config.js (the PM2 shape to adapt — name the p1 file .cjs):
```js
module.exports = {
  apps: [{
    name: 'cotizador-bot',
    script: '/opt/cotizador-bot/.venv/bin/uvicorn',
    args: 'api.server:app --host 0.0.0.0 --port 8090 --workers 1',
    cwd: '/opt/cotizador-bot',
    interpreter: 'none',
    max_memory_restart: '2G',
    autorestart: true,
    watch: false,
    error_file: '/var/log/cotizador-bot.err.log',
    out_file: '/var/log/cotizador-bot.out.log',
    time: true,
  }]
}
```

cotizador-bot/deploy.sh key pieces to adapt:
- VPS_HOST=148.230.72.17, VPS_USER=root, SSH_KEY=$HOME/.ssh/deploy_vps, SSH_OPTS="-i $SSH_KEY -o StrictHostKeyChecking=no"
- `rsync -avz --delete -e "ssh $SSH_OPTS" --exclude=... ./ "$VPS_USER@$VPS_HOST:$VPS_PATH/"`
- remote heredoc: copy creds from /opt/hub/.env.local if .env.local missing, install deps, pm2 start/restart, pm2 save
- the cotizador health check uses a RETRY LOOP around `curl http://127.0.0.1:8090/health` (up to 15×2s). ADAPT this retry pattern but REPLACE the curl with a pm2-online + stable-restart-count check, since p1-scraper is a cron worker not an HTTP server.
</reference_patterns>

<interfaces>
From p1-scraper/src/config.ts — after Task 1 it resolves env from candidate paths
(`<projectRoot>/.env.local` then `<projectRoot>/../.env.local`) using fs.existsSync.

From p1-scraper/src/run-scrape.ts — it currently runs `main()` on import (top-level
`main().catch(...)`). The cron worker should trigger this pipeline. Two acceptable
approaches (pick the cleaner one and document it in the SUMMARY):
  (A) Refactor run-scrape.ts to `export async function runScrape() { ... }` and keep
      the `main()`/CLI guard so `npm run scrape` still works, then import runScrape in cron.ts.
  (B) Have cron.ts spawn the existing entrypoint: `spawn('npx', ['tsx', 'src/run-scrape.ts'], { stdio: 'inherit' })`.
Approach B is lower-risk (no refactor of the verified Phase 1 entrypoint). Prefer B
unless A is trivially clean.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make config.ts env loading portable across both VPS and hub layouts</name>
  <files>p1-scraper/src/config.ts</files>
  <read_first>
    - p1-scraper/src/config.ts (current env resolution — single hardcoded ../../.env.local)
  </read_first>
  <action>
    The current config.ts resolves the env file at a fixed relative offset
    (`../../.env.local` from src/config.ts), which lands at `hub/.env.local` locally
    but at `/opt/.env.local` on the VPS — where p1-scraper is deployed STANDALONE to
    /opt/p1-scraper. deploy.sh writes creds to /opt/p1-scraper/.env.local, so the
    process crashes "Missing Supabase credentials". Fix it to work in BOTH layouts.

    Update `p1-scraper/src/config.ts` so it:
      - Resolves `projectRoot` = the p1-scraper directory from import.meta.url
        (src/config.ts → src → p1-scraper):
        `const projectRoot = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..')`
      - Defines an ARRAY of candidate .env.local paths, tried in order:
        ```ts
        const envCandidates = [
          path.join(projectRoot, '.env.local'),        // standalone VPS: /opt/p1-scraper/.env.local
          path.join(projectRoot, '..', '.env.local'),  // nested in hub: hub/.env.local
        ]
        ```
      - Loads the FIRST that exists via fs.existsSync:
        `const envPath = envCandidates.find((p) => existsSync(p)); if (envPath) loadEnv({ path: envPath })`
      - Keeps the fail-fast validation: if NEXT_PUBLIC_SUPABASE_URL or
        SUPABASE_SERVICE_ROLE_KEY are still missing after loading, throw an Error
        (include the candidate list in the message for debuggability).
      - Adds `import { existsSync } from 'node:fs'`.

    Leave THROTTLE_MS, BROWSER_UA, and the exported `config` object unchanged in shape.
  </action>
  <verify>
    <automated>cd /Users/ezequielmayoni/hub/p1-scraper && npx tsc --noEmit -p tsconfig.json && npm run scrape:dry >/dev/null 2>&1 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "existsSync" p1-scraper/src/config.ts` (candidate existence check)
    - `grep -qE "envCandidates|candidates" p1-scraper/src/config.ts` (array of candidate paths)
    - config.ts still throws when both creds are missing (fail-fast preserved)
    - `npm run scrape:dry` runs locally WITHOUT a "Missing Supabase credentials" crash (loads hub/.env.local via candidate #2)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>config.ts loads the first existing of the candidate .env.local paths via existsSync, works standalone on the VPS (/opt/p1-scraper/.env.local) and nested in the hub locally, keeps fail-fast validation, and `npm run scrape:dry` still works locally.</done>
</task>

<task type="auto">
  <name>Task 2: Add node-cron dep + create src/cron.ts daily scheduler</name>
  <files>p1-scraper/src/cron.ts, p1-scraper/package.json, p1-scraper/.env.example</files>
  <read_first>
    - p1-scraper/src/run-scrape.ts (the entrypoint the cron triggers; note top-level main())
    - p1-scraper/package.json (scripts + deps)
  </read_first>
  <action>
    Add `node-cron` to dependencies and `@types/node-cron` to devDependencies in
    p1-scraper/package.json. Run `cd /Users/ezequielmayoni/hub/p1-scraper && npm install node-cron && npm install -D @types/node-cron`.

    Create `p1-scraper/src/cron.ts` (ESM). It is a LONG-LIVED process suitable for PM2:
      - `import cron from 'node-cron'`
      - `import { spawn } from 'node:child_process'`  (Approach B — spawn the existing entrypoint)
      - Define `function runScrapeOnce(): void` that:
          - logs `[cron] starting scrape run ' + new Date().toISOString()`
          - `const child = spawn('npx', ['tsx', 'src/run-scrape.ts'], { stdio: 'inherit', cwd: process.cwd() })`
          - on 'exit': log `[cron] scrape run exited code=' + code`
          - guard against overlap with a module-level `let running = false`: if running, log `[cron] previous run still in progress — skipping` and return; set running=true on spawn, false on exit.
      - Schedule: `cron.schedule('0 6 * * *', runScrapeOnce, { timezone: 'Europe/Madrid' })`
        (06:00 daily; OPS-01 = 1×/day). Use Europe/Madrid to match the catalog's locale.
      - Log on startup: `[cron] p1-scraper scheduler started — daily at 06:00 Europe/Madrid`
      - Optional: if `process.argv.includes('--now')`, call runScrapeOnce() immediately
        (useful for the deploy verification: force a run without waiting for 06:00).
      - Keep the process alive: node-cron keeps the event loop busy, so no extra keep-alive needed.
      - Do NOT call process.exit — this process must stay running under PM2.

    Add to package.json "scripts": `"cron": "npx tsx src/cron.ts"`.

    In p1-scraper/.env.example add a comment noting the bucket dependency, e.g.:
      `# Storage bucket p1-images (public) must exist — created by src/images.ts ensureBucket or manually in the dashboard`
  </action>
  <verify>
    <automated>cd /Users/ezequielmayoni/hub/p1-scraper && npx tsc --noEmit -p tsconfig.json && node -e "const p=require('./package.json'); if(!p.scripts.cron||!p.dependencies['node-cron']) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "cron.schedule" p1-scraper/src/cron.ts`
    - `grep -qE "'0 6 \\* \\* \\*'" p1-scraper/src/cron.ts` (daily 06:00 schedule)
    - `grep -q "spawn" p1-scraper/src/cron.ts` (Approach B) — OR runScrape import if Approach A chosen
    - `grep -q '"cron"' p1-scraper/package.json` and node-cron in dependencies
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>node-cron installed; src/cron.ts schedules a daily run at 06:00 Europe/Madrid, prevents overlapping runs, stays alive for PM2; `npm run cron` script present; type-checks clean.</done>
</task>

<task type="auto">
  <name>Task 3: ecosystem.config.cjs (PM2) + deploy.sh (rsync + npm ci + PM2 + retry health)</name>
  <files>p1-scraper/ecosystem.config.cjs, p1-scraper/deploy.sh</files>
  <read_first>
    - cotizador-bot/ecosystem.config.js (PM2 shape to adapt)
    - cotizador-bot/deploy.sh (rsync + remote setup + creds copy + pm2 + retry health loop — adapt, drop Python/curl-health)
    - p1-scraper/src/cron.ts (the process PM2 runs)
  </read_first>
  <action>
    Create `p1-scraper/ecosystem.config.cjs` — name it `.cjs` (NOT `.js`) from the
    start, because package.json has `type: module` and PM2 reads the config with
    `require()`/`module.exports`; a `.js` file under type:module is treated as ESM and
    `module.exports` can fail. The `.cjs` extension forces CommonJS. Define:
      ```js
      module.exports = {
        apps: [{
          name: 'p1-scraper',
          script: 'npx',
          args: 'tsx src/cron.ts',
          cwd: '/opt/p1-scraper',
          interpreter: 'none',
          max_memory_restart: '500M',
          autorestart: true,
          watch: false,
          error_file: '/var/log/p1-scraper.err.log',
          out_file: '/var/log/p1-scraper.out.log',
          time: true,
        }]
      }
      ```
    (No Playwright/browser, so 500M is plenty vs cotizador's 2G.)

    Create `p1-scraper/deploy.sh` (bash, `set -euo pipefail`, chmod +x). Adapt from
    cotizador-bot/deploy.sh:
      - VPS_HOST=148.230.72.17, VPS_USER=root, VPS_PATH=/opt/p1-scraper, APP_NAME=p1-scraper,
        SSH_KEY=$HOME/.ssh/deploy_vps, SSH_OPTS="-i $SSH_KEY -o StrictHostKeyChecking=no".
      - rsync the p1-scraper folder:
        `rsync -avz --delete -e "ssh $SSH_OPTS" --exclude='node_modules' --exclude='.git/' --exclude='.env.local' --exclude='.env' --exclude='*.png' --exclude='.planning/' ./ "$VPS_USER@$VPS_HOST:$VPS_PATH/"`
        (Run rsync with the p1-scraper folder as the source — the script lives in it,
        so `./` is correct when deploy.sh is invoked from p1-scraper/. Document that
        deploy.sh must be run from inside p1-scraper/.)
      - Remote heredoc (`ssh $SSH_OPTS "$VPS_USER@$VPS_HOST" bash <<'REMOTE' ... REMOTE`):
          - `cd /opt/p1-scraper`
          - If `.env.local` missing: if `/opt/hub/.env.local` exists, copy the needed keys
            INTO /opt/p1-scraper/.env.local (this is config.ts candidate #1 on the VPS):
            `grep -E "^(NEXT_PUBLIC_SUPABASE_URL|SUPABASE_SERVICE_ROLE_KEY|TC_API_BASE_URL|TC_USERNAME|TC_PASSWORD|TC_MICROSITE_ID|TC_SUPPLIER_ID)=" /opt/hub/.env.local > .env.local`
            else echo error and exit 1.
            The written file lands at /opt/p1-scraper/.env.local, which config.ts (Task 1)
            resolves as the FIRST candidate path on the VPS. No further path note needed —
            the standalone layout is the primary VPS layout.
          - `npm ci` (or `npm install` if no package-lock; prefer npm ci — ensure package-lock.json is committed/rsynced. If lockfile absent, fall back to npm install.)
          - Ensure tsx available (it is a devDependency; npm ci installs devDeps by default — good).
          - PM2: `if pm2 list | grep -q "p1-scraper"; then pm2 restart p1-scraper --update-env; else pm2 start ecosystem.config.cjs && pm2 save; fi`
          - Health (cron worker, NOT http) — RETRY LOOP mirroring cotizador's health
            pattern, but checking pm2 online + STABLE restart count instead of curl:
            ```
            # Capture baseline restart count, then poll: healthy = online AND not crash-looping
            BASE=$(pm2 jlist | node -e "let d='';process.stdin.on('data',c=>d+=c).on('end',()=>{const a=JSON.parse(d).find(x=>x.name==='p1-scraper');process.stdout.write(String(a?a.pm2_env.restart_time:'NA'))})")
            OK=0
            for i in $(seq 1 10); do
              sleep 2
              LINE=$(pm2 jlist | node -e "let d='';process.stdin.on('data',c=>d+=c).on('end',()=>{const a=JSON.parse(d).find(x=>x.name==='p1-scraper');process.stdout.write(a?a.pm2_env.status+' '+a.pm2_env.restart_time:'missing 0')})")
              STATUS=$(echo "$LINE" | awk '{print $1}')
              RST=$(echo "$LINE" | awk '{print $2}')
              if [ "$STATUS" = "online" ] && [ "$RST" = "$BASE" ]; then
                echo "✅ p1-scraper online and stable (no new restarts) — attempt $i"
                OK=1
                break
              fi
              echo "… attempt $i: status=$STATUS restarts=$RST (baseline=$BASE)"
            done
            if [ "$OK" != "1" ]; then
              echo "❌ p1-scraper not stably online (crash-looping or down) — check pm2 logs p1-scraper" >&2
              pm2 logs p1-scraper --lines 30 --nostream || true
              exit 1
            fi
            pm2 status p1-scraper
            ```
            (If extracting restart_time via node is awkward, an acceptable simpler
            variant is: loop up to 10×2s requiring `pm2 list | grep "p1-scraper.*online"`
            for TWO consecutive polls — a steady-online window proves it is not
            crash-looping. Either approach satisfies the retry+stability requirement;
            do NOT use the single `sleep 3` + one grep.)
      - Final echo: how to view logs (`ssh root@148.230.72.17 'pm2 logs p1-scraper'`) and
        how to force a run (`ssh root@148.230.72.17 'cd /opt/p1-scraper && npx tsx src/cron.ts --now'` or via a one-off `npm run scrape`).

    `chmod +x p1-scraper/deploy.sh`.
  </action>
  <verify>
    <automated>cd /Users/ezequielmayoni/hub && bash -n p1-scraper/deploy.sh && node -e "require('./p1-scraper/ecosystem.config.cjs')" && test -x p1-scraper/deploy.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "name: 'p1-scraper'" p1-scraper/ecosystem.config.cjs`
    - `grep -q "tsx src/cron.ts" p1-scraper/ecosystem.config.cjs`
    - `grep -q "/var/log/p1-scraper" p1-scraper/ecosystem.config.cjs`
    - `grep -q "ecosystem.config.cjs" p1-scraper/deploy.sh` (deploy references the .cjs file)
    - `grep -q "/opt/p1-scraper" p1-scraper/deploy.sh`
    - `grep -q "/opt/p1-scraper/.env.local" p1-scraper/deploy.sh` OR creds grep writes to `.env.local` while cwd is /opt/p1-scraper (creds land at /opt/p1-scraper/.env.local)
    - `grep -q "rsync" p1-scraper/deploy.sh` and `grep -q "npm ci" p1-scraper/deploy.sh`
    - `grep -q "/opt/hub/.env.local" p1-scraper/deploy.sh` (creds source)
    - `grep -qE "for i in \\\$\\(seq 1 10\\)|seq 1 10" p1-scraper/deploy.sh` (retry loop, not single sleep 3)
    - `grep -q "pm2" p1-scraper/deploy.sh` and NO `curl.*health` line (worker, not HTTP)
    - `bash -n p1-scraper/deploy.sh` exits 0 (syntax valid); ecosystem.config.cjs requires cleanly
    - deploy.sh is executable
  </acceptance_criteria>
  <done>ecosystem.config.cjs defines the p1-scraper PM2 app running tsx src/cron.ts with logs/restart limits; deploy.sh rsyncs to /opt/p1-scraper, writes creds to /opt/p1-scraper/.env.local, npm ci, pm2 start ecosystem.config.cjs/restart + save, and verifies pm2-online with a retry loop checking stable restart count (no HTTP health check). Both pass syntax/require checks.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>
    config.ts now loads creds portably (standalone-VPS path first), src/cron.ts
    (daily scheduler), ecosystem.config.cjs (PM2 app), and deploy.sh (rsync + npm ci
    + PM2 + retry health) are all in place and pass local syntax/type checks.
    Plan 01 already added image mirroring to the pipeline.
  </what-built>
  <how-to-verify>
    The actual VPS deploy requires SSH access to root@148.230.72.17 (key ~/.ssh/deploy_vps)
    and runs against the live VPS + PM2 — Claude cannot self-authenticate this. Run:

    1. From inside the p1-scraper folder:
         cd /Users/ezequielmayoni/hub/p1-scraper && ./deploy.sh
       Expect: rsync completes, remote npm ci succeeds, creds written to
       /opt/p1-scraper/.env.local, "✅ p1-scraper online and stable" printed,
       pm2 status shows p1-scraper online (and NOT crash-looping).

    2. Confirm the scheduler started:
         ssh -i ~/.ssh/deploy_vps root@148.230.72.17 'pm2 logs p1-scraper --lines 20 --nostream'
       Expect: line "[cron] p1-scraper scheduler started — daily at 06:00 Europe/Madrid".
       Also confirm there is NO "Missing Supabase credentials" crash (config.ts loaded
       /opt/p1-scraper/.env.local).

    3. Force a real run and confirm fresh DB writes (OPS-01 + IMG + DATA-03 end-to-end):
         ssh -i ~/.ssh/deploy_vps root@148.230.72.17 'cd /opt/p1-scraper && npx tsx src/run-scrape.ts'
       Then in Supabase SQL:
         - select count(*) from p1_events where main_image_storage_path is not null;  -- > 0
         - select count(*) from p1_tickets where seatplan_image_storage_path is not null;  -- > 0
         - select count(*) from p1_price_history where recorded_at > now() - interval '1 hour';  -- > 0 (one row per ticket this run)
       And confirm objects exist under events/ and tickets/ in the p1-images bucket
       (Supabase dashboard → Storage → p1-images).

    4. If the createBucket API did not create p1-images, create it manually:
       Supabase dashboard → Storage → New bucket → name `p1-images`, mark PUBLIC, then re-run step 3.
  </how-to-verify>
  <resume-signal>Type "approved" when pm2 shows p1-scraper online and stable (no crash-loop), there is no missing-credentials error, and the forced run produced storage_path values + fresh p1_price_history rows; or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local → VPS over SSH | deploy.sh pushes code and runs remote commands as root |
| VPS env file | Supabase service-role + TC creds copied from /opt/hub/.env.local into /opt/p1-scraper/.env.local |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Spoofing | SSH to VPS | mitigate | Fixed host 148.230.72.17 with deploy key ~/.ssh/deploy_vps; deploy is a human-action checkpoint run by the operator. |
| T-02-06 | Information disclosure | creds copied to /opt/p1-scraper/.env.local | mitigate | rsync excludes .env.local from upload; creds sourced on-VPS from /opt/hub/.env.local; file stays under /opt root-owned. Never echoed in logs. |
| T-02-07 | Denial of Service | overlapping daily runs piling up | mitigate | cron.ts `running` guard skips a new run if the previous is still in progress; PM2 max_memory_restart caps memory. |
| T-02-08 | Tampering | running arbitrary code as root on VPS | accept | Operator-controlled deploy from a trusted machine; same posture as existing cotizador-bot deploy. |
</threat_model>

<verification>
- `npm run scrape:dry` works locally (config.ts loads hub/.env.local via candidate #2).
- `bash -n deploy.sh` and `node -e "require('./ecosystem.config.cjs')"` pass locally.
- `npx tsc --noEmit` passes (config.ts + cron.ts type-check).
- After deploy (checkpoint): `pm2 list` shows p1-scraper online and stable; pm2 logs show the
  scheduler start line with NO missing-credentials crash; a forced run writes storage_path values + fresh price_history rows.
</verification>

<success_criteria>
- config.ts loads creds portably across standalone-VPS and nested-hub layouts (candidate paths + existsSync, fail-fast preserved).
- src/cron.ts schedules the scrape pipeline daily (OPS-01) and runs as a long-lived PM2 process.
- ecosystem.config.cjs + deploy.sh provide a reproducible Node deploy to the VPS (OPS-02), with a retry-based stability health check.
- Deploy verified live: p1-scraper online and stable under PM2, scheduler started, forced run produces images in Storage + price snapshots.
</success_criteria>

<output>
After completion, create `.planning/phases/02-imagenes-scheduling-deploy/02-02-SUMMARY.md`
</output>
